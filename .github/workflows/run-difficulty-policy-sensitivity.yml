name: Run difficulty policy sensitivity

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref to measure"
        required: false
        default: "main"
        type: string
      runs:
        description: "Runs per starting-kit/workshop/policy condition (minimum 1000)"
        required: false
        default: "1000"
        type: string
      seed:
        description: "Deterministic matched world-seed input"
        required: false
        default: "1277"
        type: string
      purpose:
        description: "Why this diagnostic is being requested"
        required: true
        type: string

permissions:
  contents: read

jobs:
  measure:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}
          fetch-depth: 0
          filter: blob:none
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Run matched Portal policy sensitivity diagnostic
        env:
          MEASUREMENT_REPOSITORY: ${{ github.repository }}
          MEASUREMENT_REQUESTED_REF: ${{ inputs.ref }}
          MEASUREMENT_WORKFLOW_RUN_ID: ${{ github.run_id }}
          MEASUREMENT_WORKFLOW_RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          MEASUREMENT_RUN_TYPE: diagnostic
        run: |
          mkdir -p /tmp/run-difficulty-policy-sensitivity
          node scratch/measurements/measure_run_difficulty.js \
            --ref "${{ inputs.ref }}" \
            --runs "${{ inputs.runs }}" \
            --seed "${{ inputs.seed }}" \
            --policies "p0,p1,p2" \
            --purpose "${{ inputs.purpose }}" \
            --output /tmp/run-difficulty-policy-sensitivity/measurement.json \
            --summary /tmp/run-difficulty-policy-sensitivity/measurement.md \
            --manifest /tmp/run-difficulty-policy-sensitivity/manifest.json
      - name: Publish diagnostic summary
        if: always()
        run: cat /tmp/run-difficulty-policy-sensitivity/measurement.md >> "$GITHUB_STEP_SUMMARY"
      - name: Publish machine-readable manifest
        if: always()
        run: |
          printf '\n### Machine-readable manifest\n\n```json\n' >> "$GITHUB_STEP_SUMMARY"
          cat /tmp/run-difficulty-policy-sensitivity/manifest.json >> "$GITHUB_STEP_SUMMARY"
          printf '```\n' >> "$GITHUB_STEP_SUMMARY"
      - name: Upload diagnostic artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: run-difficulty-policy-sensitivity-${{ github.run_id }}
          path: |
            /tmp/run-difficulty-policy-sensitivity/measurement.json
            /tmp/run-difficulty-policy-sensitivity/measurement.md
            /tmp/run-difficulty-policy-sensitivity/manifest.json
          if-no-files-found: error
          retention-days: 90
