name: Early run attrition trajectory

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref to measure"
        required: false
        default: "main"
        type: string
      runs:
        description: "Runs per starting-kit/workshop/policy condition (minimum 1000)"
        required: false
        default: "1000"
        type: string
      seed:
        description: "Deterministic production simulation seed"
        required: false
        default: "1277"
        type: string
      purpose:
        description: "Why this diagnostic is being requested"
        required: true
        type: string

permissions:
  contents: read

jobs:
  measure:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Run production-backed B1-B5 trajectory diagnostic
        env:
          MEASUREMENT_REPOSITORY: ${{ github.repository }}
          MEASUREMENT_REQUESTED_REF: ${{ inputs.ref }}
          MEASUREMENT_WORKFLOW_RUN_ID: ${{ github.run_id }}
          MEASUREMENT_WORKFLOW_RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          MEASUREMENT_RUN_TYPE: diagnostic
        run: |
          mkdir -p /tmp/early-run-attrition-trajectory
          node scratch/measurements/measure_early_run_attrition_trajectory.js \
            --ref "${{ inputs.ref }}" \
            --runs "${{ inputs.runs }}" \
            --seed "${{ inputs.seed }}" \
            --purpose "${{ inputs.purpose }}" \
            --output /tmp/early-run-attrition-trajectory/measurement.json \
            --summary /tmp/early-run-attrition-trajectory/measurement.md \
            --manifest /tmp/early-run-attrition-trajectory/manifest.json
      - name: Publish trajectory summary
        if: always()
        run: cat /tmp/early-run-attrition-trajectory/measurement.md >> "$GITHUB_STEP_SUMMARY"
      - name: Publish machine-readable manifest
        if: always()
        run: |
          printf '\n### Machine-readable manifest\n\n```json\n' >> "$GITHUB_STEP_SUMMARY"
          cat /tmp/early-run-attrition-trajectory/manifest.json >> "$GITHUB_STEP_SUMMARY"
          printf '```\n' >> "$GITHUB_STEP_SUMMARY"
      - name: Upload trajectory artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: early-run-attrition-trajectory-${{ github.run_id }}
          path: |
            /tmp/early-run-attrition-trajectory/measurement.json
            /tmp/early-run-attrition-trajectory/measurement.md
            /tmp/early-run-attrition-trajectory/manifest.json
          if-no-files-found: error
          retention-days: 90
